Whenever you want to run python script remember to use `uv run` not just `python` -- you can of course do `uv run python` if you need a repl.

When you need to install a Python package, use the `/install` slash command.